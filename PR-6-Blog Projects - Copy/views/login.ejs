<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login User</title>
    <style>
        /* Reset Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        /* Body Styling */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #fff6da66;
        }

        /* Login Card */
        .login-container {
            width: 380px;
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.15);
            text-align: center;
            border-top: 6px solid #7d5243;
        }

        /* Header */
        .login-container h1 {
            color: #7d5243;
            margin-bottom: 20px;
            font-size: 24px;
            font-weight: 600;
        }

        /* Form Group */
        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            font-weight: 500;
            color: #7d5243;
            margin-bottom: 6px;
            font-size: 15px;
        }

        input {
            width: 100%;
            padding: 12px;
            border: 2px solid #d1c4b9;
            border-radius: 8px;
            font-size: 16px;
            color: #333;
            outline: none;
            transition: all 0.3s ease;
        }

        input:focus {
            border-color: #7d5243;
            box-shadow: 0 0 6px rgba(125, 82, 67, 0.3);
        }

        /* Button */
        .btn {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 8px;
            background: #7d5243;
            color: white;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 10px;
        }

        .btn:hover {
            background: #5a3d2e;
            transform: translateY(-2px);
        }

        /* Register Link */
        .register-link {
            display: block;
            margin-top: 15px;
            color: #7d5243;
            font-size: 15px;
            font-weight: 500;
            text-decoration: none;
            transition: 0.3s ease;
        }

        .register-link:hover {
            text-decoration: underline;
            color: #5a3d2e;
        }

        /* Responsive Design */
        @media (max-width: 420px) {
            .login-container {
                width: 90%;
                padding: 25px;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>Login User</h1>
        <form action="/loginuser" method="post">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="text" id="email" name="email" placeholder="Enter your email" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" placeholder="Enter your password" required>
            </div>
            <button type="submit" class="btn">Login</button>
        </form>
        <a href="/register" class="register-link">Don't have an account? Register</a>
    </div>
</body>
</html>
