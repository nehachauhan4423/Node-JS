
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Movies</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <style>
        /* Basic Styles */
        
        body {
            font-family: sans-serif;
            background-color: #2D1B2E;
        }

        .movie-container {
            padding: 20px;
        }

        .header-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 30px;
        }

        /* Card Styles */
        .card {
            border-radius: 15px;
            overflow: hidden;
            transition: transform 0.4s ease, box-shadow 0.4s ease;
            background-color: #fff;
            margin-bottom: 20px;
            width: 350px;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0px 18px 35px rgba(0, 0, 0, 0.25);
        }

        .card-img-top {
            height: 300px;
            object-fit: cover;
            transition: transform 0.5s ease, filter 0.4s ease;
        }

        .card:hover .card-img-top {
            transform: translateY(-8px) scale(1.05);
            filter: brightness(85%);
        }

        .card-body {
            padding: 28px 0 0 32px;
        }

        .card-title {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-text {
            margin-bottom: 10px;
        }

        .card-footer {
            padding: 15px;
            background-color: #f8f9fa;
            border-top: none;
        }

        /* Button Styles */
        .btn- {
            transition: background-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-danger:hover {
            background-color:  #9A506D;
            transform: scale(1.05);
            box-shadow: 0px 0px 5px rgb(255, 255, 255);
            transition: 0.5s;
        }

        .btn-success:hover {
            transform: scale(1.1);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .col-md-4 {
                flex: 0 0 50%;
                max-width: 50%;
            }
        }

        @media (max-width: 576px) {
            .col-md-4 {
                flex: 0 0 100%;
                max-width: 100%;
            }
        }
        .card {
            opacity: 0; /* Initially hidden */
            transform: translateY(20px); /* Move down initially */
            animation: fadeInUp 0.8s ease-out forwards; /* Apply animation */
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        h2{
            color: #fff;
            margin-left: 50px;
            font-size: 30px;
            font-family: sans-serif;

        }
    </style>
</head>

<body>
    <div class="container mt-4 movie-container align-items-center">
        <div class="header-row">
            <h2>Watch Movie</h2>
            <a href="/crud/add" class="btn btn-danger"> Add Movie</a>
        </div>

        <div class="row mt-4">
            <% record.map((val, index) => { %>
                <div class="col-md-4 col-sm-6  mt-3 animate__animated animate__fadeInUp">
                    <div class="card h-100 shadow m-3">
                        <img src="<%= val?.image %>" class="card-img-top" alt="Movie Image">
                        <div class="card-body">
                            <h5 class="card-title"><strong>Movie Name:</strong> <%= val?.name %></h5>
                            <p class="card-text"><strong>Description:</strong> <%= val?.discription %></p>
                            <p class="card-text"><strong>Price:</strong> ₹<%= val?.price %></p>
                            <p class="card-text"><strong>Type:</strong> <%= val?.hobby %></p>
                        </div>
                        <div class="card-footer text-center">
                            <a href="/crud/deleterecord?id=<%= val?.id %>" class="btn btn-danger btn-sm" style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;font-size: 20px;width:80px;">
                                <i class="fas fa-trash-alt"></i> Delete
                            </a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>