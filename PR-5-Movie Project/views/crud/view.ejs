<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Users</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>
    <style>
    /* Card Styling */
.card {
    position: relative;
    border-radius: 15px;
    overflow: hidden;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
    background-color: #fff;
}

/* Hover Effect: Smooth Zoom & Shadow */
.card:hover {
    transform: scale(1.05);
    box-shadow: 0px 18px 35px rgba(0, 0, 0, 0.25);
}

/* Image Hover Effect: Moves Up */
.card-img-top {
    transition: transform 0.5s ease, filter 0.4s ease;
}

.card:hover .card-img-top {
    transform: translateY(-8px) scale(1.05);
    filter: brightness(85%);
}

/* Glow Effect using ::before */
.card::before {
    content: "";
    position: absolute;
    inset: -5px;
    border-radius: 15px;
    background: linear-gradient(45deg, rgba(255, 0, 150, 0.3), rgba(0, 255, 150, 0.3));
    opacity: 0;
    transition: opacity 0.5s ease-in-out, transform 0.4s ease;
    z-index: -1;
}

/* Glowing Border on Hover */
.card:hover::before {
    opacity: 1;
    transform: scale(1.1);
    box-shadow: 0px 0px 25px rgba(255, 0, 150, 0.5);
}

/* Light Overlay using ::after */
.card::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    width: 140%;
    height: 140%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 10%, transparent);
    transform: translate(-50%, -50%) scale(0.9);
    opacity: 0;
    transition: opacity 0.5s ease, transform 0.4s ease;
    z-index: -1;
}

/* Smooth Glow Expansion on Hover */
.card:hover::after {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.2);
}

/* Text Hover Animation */
.card-body {
    transition: transform 0.4s ease, opacity 0.4s ease;
}

.card:hover .card-body {
    transform: translateY(-5px);
    opacity: 1;
}

/* Delete Button Hover Effect */
.btn-danger {
    transition: background-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
}

.btn-danger:hover {
    background-color: #c82333;
    transform: scale(1.1);
    box-shadow: 0px 0px 15px rgba(255, 0, 0, 0.5);
}

    </style>
<body>
    <div class="container mt-4">
        <h2 class="text-center mb-4">Watch Movie</h2>

        <div class="row">
            <% record.map((val,index) => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow">
                        <img src="<%= val?.image %>" class="card-img-top" alt="User Image" style="height: 300px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title"><strong>Movie Name:</strong><%= val?.name %></h5>
                            <p class="card-text"><strong>Movie Discription:</strong> <%= val?.discription %></p>
                            <p class="card-text"><strong>Movie Price:</strong> ₹<%= val?.price %></p>
                            <p class="card-text"><strong>Movie Types:</strong> <%= val?.hobby %></p>
                        </div>
                        <div class="card-footer text-center">
                            <a href="/crud/deleterecord?id=<%= val?.id %>" class="btn btn-danger btn-sm">Delete</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="text-center mt-4">
            <a href="/crud/add" class="btn btn-success">Add New Movie</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
