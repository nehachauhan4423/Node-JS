<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<style>
    /* Body Styling */
    body {
        background-image: url("https://assets.nflxext.com/ffe/siteui/vlv3/fb5cb900-0cb6-4728-beb5-579b9af98fdd/web/IN-en-20250127-TRIFECTA-perspective_cf66f5a3-d894-4185-9106-5f45502fc387_large.jpg");
        width: 100%;
        margin: 0;
        font-family: 'Arial', sans-serif; /* Set a clean font */
        color: #333;
    }

    /* Table Styling */
    table {
        width: 100%;
        max-width: 700px;  /* Perfect width for better readability */
        border-collapse: collapse;
        margin: 20px auto;
        background: rgba(255, 255, 255, 0.95); /* Soft glass effect */
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        backdrop-filter: blur(10px);
    }

    /* Table Header */
    table thead {
        background: linear-gradient(135deg, #4A00E0, #8E2DE2); /* Premium gradient */
        color: white;
        text-align: left;
        font-size: 18px;
        font-weight: bold;
        text-transform: uppercase;
    }

    table thead th {
        padding: 14px 18px;
        letter-spacing: 1px; /* Add spacing for a modern look */
    }

    /* Table Rows */
    table tbody tr {
        transition: background 0.3s ease, transform 0.2s ease;
    }

    table tbody tr:nth-child(even) {
        background: rgba(0, 0, 0, 0.03);
    }

    table tbody tr:hover {
        background: rgba(78, 0, 204, 0.1);
        transform: scale(1.01);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); /* Add shadow on hover */
    }

    /* Table Cells */
    table td {
        padding: 14px 18px;
        font-size: 16px;
        color: #333;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }

    table tr:last-child td {
        border-bottom: none;
    }

    /* Input Fields Inside Table */
    input[type="text"], 
    input[type="number"], 
    input[type="file"] {
        width: 100%;  /* Makes input fill the cell */
        max-width: 300px;  /* Prevents excessive stretching */
        padding: 12px;
        margin: 5px 0;
        border-radius: 8px;
        border: 1px solid #ccc;
        font-size: 15px;
        background: rgba(255, 255, 255, 0.9);
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    input[type="text"]:focus, 
    input[type="number"]:focus, 
    input[type="file"]:focus {
        border-color: #8E2DE2;
        box-shadow: 0 0 8px rgba(142, 45, 226, 0.4);
        outline: none;
    }

    /* Button Styling */
    input[type="submit"] {
        background: #4A00E0;
        color: white;
        padding: 14px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 16px;
        width: 100%;
        max-width: 300px;
        transition: background 0.3s ease, transform 0.2s ease;
        font-weight: bold; /* Make button text bold */
    }

    input[type="submit"]:hover {
        background: #3700B3;
        transform: scale(1.05);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Add shadow on hover */
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        table {
            width: 90%; /* Adjust table width on smaller screens */
        }
    }
        /* Responsive Design */
        @media (max-width: 768px) {
        table {
            width: 90%; /* Adjust table width on smaller screens */
        }

        table thead {
            font-size: 16px; /* Slightly smaller font for mobile */
        }

        table td {
            font-size: 14px; /* Slightly smaller font for mobile */
        }

        input[type="submit"] {
            max-width: 100%; /* Full width on mobile */
        }
    }

    /* Additional Styling for Table */
    table {
        border: 1px solid rgba(0, 0, 0, 0.1); /* Add a subtle border around the table */
    }

    /* Table Header Cells */
    table thead th {
        border-bottom: 2px solid rgba(255, 255, 255, 0.3); /* Distinct border for header */
    }

    /* Table Row Hover Effect */
    table tbody tr:hover {
        background: rgba(78, 0, 204, 0.2); /* Lighter hover effect */
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Shadow on hover */
    }

    /* Input Fields Styling */
    input[type="text"], 
    input[type="number"], 
    input[type="file"] {
        font-family: 'Arial', sans-serif; /* Consistent font for inputs */
        font-weight: normal; /* Normal weight for input text */
    }

    /* Button Focus Effect */
    input[type="submit"]:focus {
        outline: none; /* Remove default outline */
        box-shadow: 0 0 8px rgba(142, 45, 226, 0.6); /* Enhanced focus effect */
    }

    /* Table Footer (if needed) */
    table tfoot {
        background: rgba(255, 255, 255, 0.9); /* Light background for footer */
        font-weight: bold; /* Bold text for footer */
    }

    table tfoot td {
        padding: 14px 18px;
        border-top: 2px solid rgba(0, 0, 0, 0.1); /* Distinct border for footer */
    }

    /* Custom Scrollbar for Table */
    table {
        overflow-y: auto; /* Enable vertical scrolling if needed */
        max-height: 400px; /* Set a max height for the table */
    }

    /* Scrollbar Styling */
    table::-webkit-scrollbar {
        width: 8px; /* Width of the scrollbar */
    }

    table::-webkit-scrollbar-thumb {
        background: rgba(78, 0, 204, 0.5); /* Color of the scrollbar thumb */
        border-radius: 10px; /* Rounded corners for the scrollbar thumb */
    }

    table::-webkit-scrollbar-thumb:hover {
        background: rgba(78, 0, 204, 0.7); /* Darker on hover */
    }

    /* Table Caption */
    table caption {
        font-size: 20px; /* Caption font size */
        font-weight: bold; /* Caption font weight */
        margin: 10px 0; /* Margin for spacing */
        color: #4A00E0; /* Caption color */
    }
</style>

<body>

    <div align="center">
        <h2>Add User</h2>
        <form action="/crud/insertrecord" method="post" enctype="multipart/form-data">
            <table border="1">
                <tr>
                    <td>Movie Name </td>
                    <td><input type="text" name="name"></td>
                </tr>
                <tr>
                    <td>Movie Discription</td>
                    <td><input type="text" name="discription"></td>
                </tr>
                <tr>
                    <td>Movie Price</td>
                    <td><input type="number" name="price"></td>
                </tr>
                <tr>
                    <td>Movie Types </td>
                    <td>
                        <input type="checkbox" name="hobby" value="Horror">Horror
                        <input type="checkbox" name="hobby" value="Action">Action
                        <input type="checkbox" name="hobby" value="Comedy">Comedy
                        <input type="checkbox" name="hobby" value="History">History
                    </td>
                </tr>
                <tr>
                    <td>Movie Image</td>
                    <td><input type="file" name="upload"></td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit"></td>
                </tr>
            </table>
        </form>
        <a href="/crud" style="color: #ffffff;">View</a>
    </div>

</body>

</html> -->




<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Movie</title>
    <style>
        body {
            font-family: sans-serif;
            background: url('https://assets.nflxext.com/ffe/siteui/vlv3/fb5cb900-0cb6-4728-beb5-579b9af98fdd/web/IN-en-20250127-TRIFECTA-perspective_cf66f5a3-d894-4185-9106-5f45502fc387_large.jpg') no-repeat center center/cover;
            color: #E2DFD0;
            text-align: center;
            padding-top: 50px;
        }

        .container {
            background: #2D1B2E; /* Dark Wine */
            padding: 30px;
            border-radius: 15px;
            width: 45%;
            margin: auto;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.8);
            transition: 0.3s ease-in-out;
            border: 2px solid #9A506D; /* Subtle Border */
        }

        h2 {
            color: #E8B4B8;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
            font-family: 'Georgia', serif;
            font-size: 32px;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 15px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            font-size: 18px;
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
            color: #E8B4B8;
        }

        input[type="text"],
        input[type="number"],
        input[type="file"] {
            width: 96%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid #E8B4B8;
            background: #4A1F38;
            color: #E2DFD0;
            font-size: 16px;
            transition: 0.3s;
            outline: none;
        }

        input[type="text"]:focus,
        input[type="number"]:focus,
        input[type="file"]:focus {
            border-color: #FF97A3;
            box-shadow: 0px 0px 8px #FF97A3;
        }

        .checkbox-group {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .checkbox-group label {
            font-size: 16px;
            font-weight: normal;
            display: flex;
            align-items: center;
            gap: 5px;
            background: #793c60;
            padding: 8px 12px;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s;
            border: 1px solid #E8B4B8;
        }

        .checkbox-group label:hover {
            background: #9A506D;
            color: white;
        }

        input[type="checkbox"] {
            transform: scale(1.3);
            accent-color: #E8B4B8;
        }

        input[type="submit"] {
            background: #9A506D;
            color: white;
            border: none;
            padding: 14px 30px;
            font-size: 18px;
            cursor: pointer;
            transition: 0.3s;
            border-radius: 12px;
            font-weight: bold;
            display: block;
            width: 100%;
            margin-top: 10px;
        }

        input[type="submit"]:hover {
            background: #C47A8D;
            box-shadow: 0px 0px 15px #FF97A3;
        }

        a {
            color: #FF97A3;
            text-decoration: none;
            font-size: 18px;
            font-weight: bold;
            display: block;
            margin-top: 15px;
        }

        a:hover {
            text-shadow: 0px 0px 10px #f8f8f8;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Add Movie</h2>
        <form action="/crud/insertrecord" method="post" enctype="multipart/form-data">
            
            <div class="form-group">
                <label for="name">Movie Name</label>
                <input type="text" id="name" name="name" placeholder="Enter Movie Name">
            </div>

            <div class="form-group">
                <label for="description">Movie Description</label>
                <input type="text" id="description" name="discription" placeholder="Enter Description">
            </div>

            <div class="form-group">
                <label for="price">Movie Price</label>
                <input type="number" id="price" name="price" placeholder="Enter Price">
            </div>

            <div class="form-group">
                <label>Movie Type</label>
                <div class="checkbox-group">
                    <label><input type="checkbox" name="hobby" value="Horror"> Horror</label>
                    <label><input type="checkbox" name="hobby" value="Action"> Action</label>
                    <label><input type="checkbox" name="hobby" value="Comedy"> Comedy</label>
                    <label><input type="checkbox" name="hobby" value="History"> History</label>
                </div>
            </div>

            <div class="form-group">
                <label for="upload">Movie Image</label>
                <input type="file" id="upload" name="upload">
            </div>

            <input type="submit" value="Add Movie">
        </form>
        <a href="/crud">View All Movies</a>
    </div>

</body>
</html>
