<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #E3F2FD, #BBDEFB);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .auth-container {
            background: white;
            padding: 35px;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 420px;
            text-align: center;
        }

        .auth-container h2 {
            margin-bottom: 20px;
            font-weight: 600;
            color: #1976D2;
        }

        .form-control {
            font-size: 1rem;
            padding: 12px;
            border-radius: 8px;
            border: 2px solid #1976D2;
            transition: 0.3s;
        }

        .form-control:focus {
            border-color: #1565C0;
            box-shadow: 0 0 8px rgba(25, 118, 210, 0.5);
        }

        .btn-custom {
            width: 100%;
            font-size: 1rem;
            font-weight: 500;
            padding: 12px;
            border-radius: 8px;
            transition: 0.3s;
        }

        .btn-custom:hover {
            opacity: 0.9;
        }

        .input-group-text {
            border-radius: 8px 0 0 8px;
            background-color: #1976D2;
            color: white;
        }

        .otp-inputs {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin: 20px 0;
        }

        .otp-inputs input {
            width: 50px;
            height: 50px;
            font-size: 20px;
            text-align: center;
            border: 2px solid #1976D2;
            border-radius: 8px;
            outline: none;
            transition: 0.3s;
        }

        .otp-inputs input:focus {
            border-color: #1565C0;
            box-shadow: 0 0 8px rgba(25, 118, 210, 0.5);
        }

        .resend-link {
            margin-top: 15px;
            font-size: 14px;
        }

        .resend-link a {
            color: #1976D2;
            text-decoration: none;
            font-weight: bold;
        }

        .resend-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="auth-container">
        <h2>Forgot Password</h2>
        <p class="text-muted">Enter your email to receive reset instructions</p>

        <form method="post" action="/forgotpassword">
            <div class="input-group mb-3">
                <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                <input type="email" name="useremail" class="form-control" placeholder="Email Address" required>
            </div>
            <button type="submit" class="btn btn-primary btn-custom">Send Instructions</button>
        </form>

        <hr>

        <h2>OTP Verification</h2>
        <p class="text-muted">Enter the 6-digit OTP sent to your email</p>

        <form method="post" action="/postotp">
            <div class="otp-inputs">
                <input type="text" maxlength="1" required>
                <input type="text" maxlength="1" required>
                <input type="text" maxlength="1" required>
                <input type="text" maxlength="1" required>
            </div>
            <button type="submit" class="btn btn-success btn-custom">Verify OTP</button>
        </form>

        <div class="resend-link">
            Didn't receive a code? <a href="#">Resend OTP</a> | <a href="/" class="text-danger">Back to Login</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
