<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* General Styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #faf3e049;
            color: #333333; /* Dark text */
            margin: 0;
            padding: 0;
            height: 100vh; /* Ensure full height */
            display: flex;
            justify-content: center;
            align-items: center; /* Centering content vertically */
            text-align: center;
        }

        h1 {
            color: #C8A165; /* Light golden brown accent */
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
            margin-top: 20px;
            animation: fadeIn 1s ease-out;
        }

        /* Centering the form */
        .form-container {
            width: 60%;
            padding: 20px;
            background-color: transparent; /* Lighter background for the form */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            animation: slideIn 1s ease-out; /* Animation for the form */
        }

        /* Table Styling */
        table {
            width: 80%;
            border-collapse: collapse;
            margin: 0 auto;
            animation: fadeIn 1.5s ease-out;
        }

        th, td {
            padding: 12px;
            text-align: left;
            color: #333333; /* Dark text color */
        }

        tr {
            transition: background-color 0.3s ease;
        }

        tr:hover {
            background-color: #f2e1c628; /* Light golden brown hover effect */
        }

        td:first-child {
            width: 30%;
            font-weight: 400;
        }

        /* Input Fields */
        input[type="text"],
        input[type="datetime-local"] {
            width: 95%;
            padding: 8px;
            border: 1px solid #C8A165; /* Light golden brown border */
            border-radius: 5px;
            background-color: #FAF3E0; /* Soft cream background for inputs */
            color: #333333; /* Dark text color */
            font-size: 14px;
            transition: all 0.3s ease-in-out; /* Smooth transition for inputs */
        }

        input[type="text"]:focus,
        input[type="datetime-local"]:focus {
            outline: none;
            border-color: #C8A165;
            box-shadow: 0 0 5px #C8A165;
            transform: scale(1.02); /* Subtle scale effect */
        }

        /* Submit Button */
        input[type="submit"] {
            padding: 10px 20px;
            background-color: #C8A165; /* Light golden brown */
            color: #FAF3E0; /* Soft cream text color */
            font-size: 14px;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            width: 100%; /* Make the button full width */
            margin-top: 10px; /* Add some spacing above */
        }

        input[type="submit"]:hover {
            background-color: #A67C4E; /* Slightly darker golden brown on hover */
            transform: scale(1.05);
        }

        /* Navigation Link */
        a {
            text-decoration: none;
            color: #C8A165; /* Light golden brown */
            font-weight: bold;
            margin-top: 20px;
            display: inline-block;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        a:hover {
            color: #A67C4E; /* Slightly darker golden brown on hover */
            transform: scale(1.1);
        }

        /* Keyframe animations */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            0% {
                opacity: 0;
                transform: translateX(-100px);
            }
            100% {
                opacity: 1;
                transform: translateX(0);
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Edit User</h1>
        <form action="/updateuser" method="post">
            <input type="hidden" name="editid" value="<%= singledata.id %>">
            <table border="1">
                <tr>
                    <td>Name</td>
                    <td>
                        <input type="text" name="username" value="<%= singledata.name %>">
                    </td>
                </tr>
                <tr>
                    <td>Phone</td>
                    <td>
                        <input type="text" name="userphone" value="<%= singledata.phone %>">
                    </td> 
                </tr>
                <tr>
                    <td>Deadline</td>
                    <td>
                        <input type="datetime-local" name="userdeadline" value="<%= singledata.deadline %>">
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td>
                        <input type="submit" value="Update User">
                    </td>
                </tr>
            </table>
        </form>
        <a href="/">View Users</a>
    </div>
</body>
</html>
